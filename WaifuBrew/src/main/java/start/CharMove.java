package start;

// For Parsing and use in AnimationPane.
public enum CharMove {
    STANDBY,
    BOW, // Down and up
    FALLLEFT, // Rotate left (and perhaps shift left)
    DOWN, // Slide down
    LEFT, // Slide left
    RIGHT // Slide right
}
